package com.loc.microservices.product_service_spring_boot.config;

import org.springframework.context.annotation.Configuration;

/**
 * This class is disabled because we're using the OpenAPI YAML file
 * located at src/main/resources/openapi.yaml instead of annotations.
 */
@Configuration
public class OpenAPIConfig {
    // OpenAPI configuration is now provided by the YAML file
}
